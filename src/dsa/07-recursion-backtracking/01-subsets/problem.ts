/**
 * Problem: Subsets (Power Set)
 * Pattern: Backtracking (choose / explore / un-choose)
 * Difficulty: Medium
 *
 * Given an array `nums` of unique integers, return all possible subsets
 * (the power set). The solution set must not contain duplicate subsets;
 * order of subsets and order within a subset does not matter.
 *
 * Example:
 *   subsets([1, 2, 3])
 *   -> [[], [1], [2], [3], [1,2], [1,3], [2,3], [1,2,3]]   (order may vary)
 *
 * Constraints:
 *  - 1 <= nums.length <= 10
 *  - All elements are unique
 *
 * There are 2^n subsets of an n-element set (each element is either "in"
 * or "out") — that count itself is a useful thing to say out loud, since it
 * tells you the answer can't be faster than O(2^n) in the worst case.
 */

export function subsets(nums: number[]): number[][] {
  // TODO: implement
  throw new Error('Not implemented')
}
