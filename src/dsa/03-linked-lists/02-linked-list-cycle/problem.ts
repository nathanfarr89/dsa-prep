/**
 * Problem: Linked List Cycle Detection
 * Pattern: Fast & Slow Pointers (Floyd's Tortoise and Hare)
 * Difficulty: Easy
 *
 * Given the head of a linked list, return `true` if the list has a cycle
 * (some node's `next` eventually points back to a previous node), `false`
 * otherwise.
 *
 * Example:
 *   1 -> 2 -> 3 -> 4 -> back to 2   =>  true
 *   1 -> 2 -> 3 -> null             =>  false
 *
 * See ../listNode.ts for the ListNode type and `arrayToListWithCycle` test
 * helper.
 *
 * Constraints:
 *  - Do this in O(1) extra space. (A Set of visited nodes works and is
 *    O(n) space — mention it as the straightforward approach, then reach
 *    for the O(1) trick.)
 */

import type { ListNode } from '../listNode'

export function hasCycle(head: ListNode | null): boolean {
  // TODO: implement
  throw new Error('Not implemented')
}
