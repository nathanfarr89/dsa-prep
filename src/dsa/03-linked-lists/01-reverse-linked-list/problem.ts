/**
 * Problem: Reverse Linked List
 * Pattern: Iterative pointer rewiring (also solvable recursively)
 * Difficulty: Easy
 *
 * Given the head of a singly linked list, reverse the list in place and
 * return the new head.
 *
 * Example:
 *   1 -> 2 -> 3 -> 4 -> 5 -> null
 *   becomes
 *   5 -> 4 -> 3 -> 2 -> 1 -> null
 *
 * See ../listNode.ts for the ListNode type and array<->list test helpers.
 *
 * Try both approaches if you have time: iterative (O(1) space) and
 * recursive (O(n) space from the call stack) — interviewers often ask for
 * the one you didn't start with as a follow-up.
 */

import type { ListNode } from '../listNode'

export function reverseList(head: ListNode | null): ListNode | null {
  // TODO: implement
  throw new Error('Not implemented')
}
