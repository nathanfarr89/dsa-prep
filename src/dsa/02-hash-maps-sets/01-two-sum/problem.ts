/**
 * Problem: Two Sum
 * Pattern: Hash Map (complement lookup)
 * Difficulty: Easy
 *
 * Given an array of integers `nums` and an integer `target`, return the
 * indices of the two numbers that add up to `target`. Assume exactly one
 * valid answer exists, and you may not use the same element twice.
 *
 * Example:
 *   twoSum([2, 7, 11, 15], 9) -> [0, 1]   (2 + 7 = 9)
 *   twoSum([3, 2, 4], 6)      -> [1, 2]   (2 + 4 = 6)
 *
 * Constraints:
 *  - 2 <= nums.length <= 10^4
 *  - Exactly one valid pair exists
 *
 * The brute force is O(n^2) (check every pair). Aim for O(n).
 */

export function twoSum(nums: number[], target: number): number[] {
  // TODO: implement
  throw new Error('Not implemented')
}
