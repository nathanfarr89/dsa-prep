/**
 * Problem: Binary Tree Level Order Traversal
 * Pattern: BFS with a queue
 * Difficulty: Medium
 *
 * Given the root of a binary tree, return the values grouped by level, from
 * top to bottom (each level as its own array, left to right).
 *
 * Example:
 *        3
 *       / \
 *      9  20
 *        /  \
 *       15   7
 *   levelOrder(root) -> [[3], [9, 20], [15, 7]]
 *
 * See ../treeNode.ts for the TreeNode type and `arrayToTree` test helper.
 *
 * This is the tree version of "process nodes in the order you discover
 * them, layer by layer" — the same shape as walking a component tree
 * breadth-first (e.g. rendering priority, or a "find the nearest X"
 * search in a DOM/UI tree).
 */

import type { TreeNode } from '../treeNode'

export function levelOrder(root: TreeNode | null): number[][] {
  // TODO: implement
  throw new Error('Not implemented')
}
