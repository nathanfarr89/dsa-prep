/**
 * Problem: Maximum Depth of Binary Tree
 * Pattern: DFS Recursion (divide and conquer)
 * Difficulty: Easy
 *
 * Given the root of a binary tree, return its maximum depth (the number of
 * nodes along the longest path from the root down to the farthest leaf).
 *
 * Example:
 *        3
 *       / \
 *      9  20
 *        /  \
 *       15   7
 *   maxDepth(root) -> 3
 *
 * See ../treeNode.ts for the TreeNode type and `arrayToTree` test helper.
 *
 * This is the "hello world" of tree recursion — every other tree/recursion
 * problem builds on this shape: handle the base case, recurse on children,
 * combine the results.
 */

import type { TreeNode } from '../treeNode'

export function maxDepth(root: TreeNode | null): number {
  // TODO: implement
  throw new Error('Not implemented')
}
