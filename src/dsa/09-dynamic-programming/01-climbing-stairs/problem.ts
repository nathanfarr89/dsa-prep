/**
 * Problem: Climbing Stairs
 * Pattern: Dynamic Programming (1D, memoized recursion or bottom-up)
 * Difficulty: Easy
 *
 * You're climbing a staircase with `n` steps. Each time you can climb
 * either 1 or 2 steps. In how many distinct ways can you climb to the top?
 *
 * Example:
 *   climbStairs(2) -> 2   (1+1, 2)
 *   climbStairs(3) -> 3   (1+1+1, 1+2, 2+1)
 *
 * Constraints:
 *  - 1 <= n <= 45
 *
 * This is the canonical "start here" DP problem — it's secretly just the
 * Fibonacci sequence, and every DP problem after this one is a variation
 * on "cache the answer to a subproblem so you don't recompute it."
 */

export function climbStairs(n: number): number {
  // TODO: implement
  throw new Error('Not implemented')
}
