/**
 * Problem: Binary Search
 * Pattern: Divide and conquer on a sorted array
 * Difficulty: Easy
 *
 * Given a sorted (ascending) array of unique integers `nums` and a
 * `target` value, return the index of `target` if it exists, otherwise -1.
 *
 * Example:
 *   binarySearch([-1, 0, 3, 5, 9, 12], 9) -> 4
 *   binarySearch([-1, 0, 3, 5, 9, 12], 2) -> -1
 *
 * Constraints:
 *  - nums is sorted in ascending order
 *  - You must write an algorithm with O(log n) runtime complexity.
 */

export function binarySearch(nums: number[], target: number): number {
  // TODO: implement
  throw new Error('Not implemented')
}
