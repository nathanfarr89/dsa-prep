/**
 * Problem: Merge Sort
 * Pattern: Divide and conquer
 * Difficulty: Medium
 *
 * Implement merge sort: given an array of numbers, return a new array with
 * the same numbers sorted in ascending order.
 *
 * Example:
 *   mergeSort([5, 2, 4, 6, 1, 3]) -> [1, 2, 3, 4, 5, 6]
 *
 * Why implement a sort you'd never use in production (Array.prototype.sort
 * exists)? Because "implement merge sort" is a very common way interviewers
 * probe whether you actually understand divide-and-conquer recursion and
 * can reason about O(n log n) — not because anyone wants you to replace the
 * built-in.
 *
 * Constraints:
 *  - Aim for O(n log n) time.
 *  - You do not need to sort in place — returning a new sorted array is fine.
 */

export function mergeSort(nums: number[]): number[] {
  // TODO: implement
  throw new Error('Not implemented')
}
